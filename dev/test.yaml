apiVersion: v1
kind: Pod
metadata:
  name: testing1
  labels:
    purpose: demonstrate-command
spec:
  containers:
  - name: command-demo
    image: centos
    imagePullPolicy: IfNotPresent
    command: ["/bin/sh"]
    args: ["-c", "while true; do echo Ahsan; sleep 100;done"]
  restartPolicy: OnFailure
